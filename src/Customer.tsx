export interface Customer {
  id: number;
  name: string;
  username: string;
  email: string;
  address: {
    street: string;
    suite: string;
    city: string;
    zipcode: string;
    geo: {
      lat: string;
      lng: string;
    };
  };
  phone: string;
  website: string;
  company: {
    name: string;
    catchPhrase: string;
    bs: string;
  };
} //I am not sure of the specifications on whether the details are sometimes ommitted. If they are ommitted then I would put some details listed in CustomerDetails as optional.